{% extends 'base_dashboard.html.twig' %}

{% block title %}EduPulse | Nouveau Formateur{% endblock %}
{% block page_title %}Ajouter un Formateur{% endblock %}

{% block body %}
<div class="max-w-2xl mx-auto space-y-6 animate-fade-in">
    <div class="flex justify-between items-center">
        <h1 class="text-2xl font-bold text-white">Nouveau Formateur</h1>
        <a href="{{ path('app_admin_teacher_index') }}" class="text-slate-400 hover:text-white transition-colors text-sm flex items-center gap-2">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
            Retour à la liste
        </a>
    </div>

    <div class="glass-card p-8">
        {{ include('admin/teacher/_form.html.twig') }}
    </div>
</div>
{% endblock %}
